// Shared, user-facing constants for the site shell.

// Corner mark that the original site places in the top-left corner overlapping
// the frame. It is kept as a single labeled constant so it is trivial to keep
// or remove. Per request, it is disabled (empty) here; set it to an emoji or
// short string to bring a corner mark back.
export const CORNER_MARK = "";

// Caption shown under the box, in a serif font.
export const CAPTION = "11:11 make a fish";

// Message shown when the 11:11 window is closed.
export const CLOSED_MESSAGE = "come back at 11:11";

// Solid blue background, essentially pure blue, matching the original.
export const BLUE = "#0000FF";
